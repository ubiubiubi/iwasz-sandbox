CMAKE_MINIMUM_REQUIRED (VERSION 2.8)
SET (CMAKE_VERBOSE_MAKEFILE of) 
SET (DEVICE msp430f5529) 

PROJECT (msp430-first)
include (msp430.cmake)

INCLUDE_DIRECTORIES("../src/")
AUX_SOURCE_DIRECTORY ("../src/" APP_SOURCES)

ADD_EXECUTABLE(${CMAKE_PROJECT_NAME}.elf ${APP_SOURCES})
#TARGET_LINK_LIBRARIES (${CMAKE_PROJECT_NAME}.elf ${TIVA_WARE_PATH}/usblib/gcc/libusb.a)
#TARGET_LINK_LIBRARIES (${CMAKE_PROJECT_NAME}.elf ${TIVA_WARE_PATH}/driverlib/gcc/libdriver.a)

FIND_PROGRAM (OPENOCD_EXECUTABLE mspdebug)
ADD_CUSTOM_TARGET("upload" DEPENDS ${CMAKE_PROJECT_NAME}.elf COMMAND ${OPENOCD_EXECUTABLE} tilib 'prog ${CMAKE_PROJECT_NAME}.elf reset')

